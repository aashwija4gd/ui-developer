# Validation Rules

The validator should reject output that:

- uses disallowed tokens or raw values
- references components not present in the registry
- violates accessibility requirements
- omits required states
- introduces unsupported formatting for currency, status, or dates

