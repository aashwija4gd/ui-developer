# CLI Workflow

1. Load the task description and repo context.
2. Read `STYLEGUIDE.md`, `ORG-STYLEGUIDE.md`, and the manifests.
3. Generate a patch or diff.
4. Run validators.
5. Repair failures and regenerate until clean or iteration limit is reached.

