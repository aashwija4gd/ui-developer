# CI Workflow

- Run validation on generated or modified UI files.
- Fail the build on rule violations.
- Store validator output as machine-readable artifacts.
- Use the repair loop before merge when generation is automated.

