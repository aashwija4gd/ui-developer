# Editor Workflow

- Trigger generation from a selected file or code region.
- Include nearby files as context.
- Write changes as a patch against the real workspace.
- Surface validator failures inline for quick repair.

